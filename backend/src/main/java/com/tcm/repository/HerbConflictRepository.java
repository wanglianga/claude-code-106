package com.tcm.repository;

import com.tcm.model.HerbConflict;
import org.springframework.data.jpa.repository.JpaRepository;

public interface HerbConflictRepository extends JpaRepository<HerbConflict, Long> {
}
